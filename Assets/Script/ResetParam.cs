using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class ResetParam : MonoBehaviour
{
    public void resetAll()
    {
        GameManager.instance.gameTime = 0;
        GameManager.instance.resource = GameManager.instance.startResource;
        GameManager.instance.redResource = GameManager.instance.startResource;
        GameManager.instance.redResourceIncrease = 5;
        GameManager.instance.upgradeLevel = 1;
        GameManager.instance.redUpgradeLevel = 1;
        GameManager.instance.increaseLevel = 1;
        GameManager.instance.redIncreaseLevel = 1;
        GameManager.instance.totalDmg = new Dictionary<string, int>();
        

        PoolManager.instance.selectedPrefabsBlue = new List<GameObject>();
        for(int i = 0; i< PoolManager.instance.mobCounts.Length; i++)
        {
            PoolManager.instance.mobCounts[i].text = "count: 0";
        }

        PoolManagerRed.instance.selectedPrefabsRed = new List<GameObject>();

        PoolManager.instance.Start();
        PoolManagerRed.instance.Start();
       


    }
}*/
